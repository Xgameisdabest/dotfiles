#!/usr/bin/env bash

# URLS=(
#   ["Google"]="https://www.google.com/search?q="
#   ["Bing"]="https://www.bing.com/search?q="
#   ["Yahoo"]="https://search.yahoo.com/search?p="
#   ["Duckduckgo"]="https://www.duckduckgo.com/?q="
#   ["Yandex"]="https://yandex.ru/yandsearch?text="
#   ["Youtube"]="https://www.youtube.com/results?search_query="
# )

web="  Web Search"
yt="  Youtube"

web(){
search=$( echo | rofi -dmenu -theme-str "window {height: 90px;}" -p " Search   " )
search_engine="https://www.google.com/search?q="
main() {
    if [[ -n "$search" ]]; then
      url="$search_engine$search"
      xdg-open "$url"
    else
		exit
	      fi
}
main
exit 0
}

yt(){
search=$( echo | rofi -dmenu -theme-str "window {height: 90px;}" -p " Search   " )
search_engine="https://www.youtube.com/results?search_query="
main() {
    if [[ -n "$search" ]]; then
      url="$search_engine$search"
      xdg-open "$url"
    else
		exit
	      fi
}
main
exit 0
}


select=$( echo -e "$web\n$yt" | rofi -dmenu -theme-str "window {height: 190px; width: 280px;}" -p " Select Search Mode ")

case $select in
	$web)
		web
		;;
	$yt)
		yt
		;;
esac
